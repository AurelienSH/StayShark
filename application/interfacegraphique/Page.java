package application.interfacegraphique;
//classe abstraite page : rien dedans
//classes filles (extend) : pageprof (a méthode reader(path)), pageélève(a méthode reader(path))
public abstract class Page {
    
}