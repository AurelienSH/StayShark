package application.contrôleur;

interface ActionHome {
  // récupération : prof --> login, vérification csv (error si po bon)
  //                élève --> login, vérification csv (error si po bon)
}